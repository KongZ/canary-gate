apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canary-gate.fullname" . }}-test-connection"
  labels:
    {{- include "canary-gate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canary-gate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
